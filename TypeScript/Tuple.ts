let myTuple : [string, number] = ['Mavis', 1];
/*
IMPORTANT: Unlike arrays, tuple data types 
need to be entered in the correct order 
that has been set., for example, if you set :
let MyTupleError : [string, number] = [1, 'Mavis'];
output : Error

*/

console.log(`${myTuple[0]}`);

//optional arguments tuple

let myTup : [string, number?];
myTup = ['Mavis'];
